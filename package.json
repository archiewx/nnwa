{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "command": {
    "publish": {
      "registry": "https://registry.npmjs.org/",
      "access": "publich"
    }
  },
  "scripts": {
    "init": "lerna run install",
    "build": "lerna run build",
    "pub": "lerna publish"
  }
}
